require 'spec_helper'

describe Impressbox do
  it 'has a version number' do
    expect(Impressbox::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
